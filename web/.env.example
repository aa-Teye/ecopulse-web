# Leave blank to run entirely on mock data.
# Once Alex's API is live, set this and the app switches automatically.
VITE_API_URL=
VITE_MAPBOX_TOKEN=

# Once Ishaque's admin dashboard is deployed, set this so the "Admin Dashboard"
# footer link points to it. Same-instance deploys can use a relative path
# like /admin instead of a full URL.
VITE_DASHBOARD_URL=
